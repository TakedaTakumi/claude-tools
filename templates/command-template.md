---
# description: コマンド一覧(補完等)に表示される1行の説明。必須。
description: <このコマンドが何をするかを1行で>
# argument-hint: /コマンド名 の後に続く引数のヒント文字列。補完時に表示される。省略可。
# 例: "[追加の指示]" や "--base=<branch>" のように、実際の引数の形に合わせる。
argument-hint: "<引数のヒント>"
# allowed-tools: このコマンドが使ってよいツールの最小権限の宣言。
# Bash は可能な限りサブコマンド単位で絞る(例: Bash(git diff:*), Bash(git status:*))。
# 無制限の Bash や不要な Write/Edit 等は与えない。
allowed-tools: <必要なツールのみをカンマ区切りで>
---

# <コマンド名>

<このコマンドの一言要約>

## 目的

<このコマンドで達成したいこと。なぜこのコマンドが必要かを1〜2文で>

## 引数仕様(`$ARGUMENTS`)

- <引数1>: <意味・形式・省略時の既定動作>
- <引数2があれば>: ...
- 不明な引数・フラグが渡された場合の扱い(例: エラー停止してユーザーに確認する。推測で進めない)

## 実行手順

1. <前提確認: 例. リポジトリ内かどうかの確認>
2. <情報収集の手順: 例. 対象コマンドで読むべきファイル・git 情報>
3. <本体の処理手順を番号付きで具体的に>
4. <例外/大規模入力時の方針切り替えがあれば明示>

## 出力形式

```
<出力の型を具体的に示す(見出し・箇条書き・表など)>
```

## 動作上の注意

- <やってはいけないこと(例: 破壊的な git 操作をしない、自動でコミットしない)>
- <推測で進めない、根拠となる情報源を明示する、等の運用ルール>
- <既存コードやリポジトリの規約を尊重する旨>
